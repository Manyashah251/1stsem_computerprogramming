#include <stdio.h>

int main() {
    int i, n, odd = 1;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("First %d odd numbers are:\n", n);
    for(i = 1; i <= n; i++) {
        printf("%d\n", odd);
        odd = odd + 2; // next odd number
    }

    return 0;
}
